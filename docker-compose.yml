version: "3.9"

services:
  backend:
    build: ./backend
    container_name: node_backend
    ports:
      - "4000:4000"

  frontend:
    build: ./frontend
    container_name: vite_frontend
    volumes:
      - ./frontend/dist:/dist
    depends_on:
      - backend
